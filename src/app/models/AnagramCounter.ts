export class AnagramCounter {
  anagramLength: string;
  anagramCounter: string;
  anagramCounterDescription: string;

  constructor(anagramLength: string, anagramCounter: string, anagramCounterDescription: string) {
    this.anagramLength = anagramLength;
    this.anagramCounter = anagramCounter;
    this.anagramCounterDescription = anagramCounterDescription;
  }
}
